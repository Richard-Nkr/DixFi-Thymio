{% extends 'base.html.twig' %}

{% block title %}ThymioChallenge{% endblock %}

{% block body %}
    <h1>ThymioChallenge</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ thymio_challenge.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ thymio_challenge.name }}</td>
            </tr>
            <tr>
                <th>Difficulty</th>
                <td>{{ thymio_challenge.difficulty }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('thymio_challenge_index') }}">back to list</a>

    <a href="{{ path('thymio_challenge_edit', {'id': thymio_challenge.id}) }}">edit</a>

    {{ include('thymio_challenge/_delete_form.html.twig') }}
{% endblock %}
