<form method="post" action="{{ path('public_challenge_delete', {'id': publicChallenge.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ publicChallenge.id) }}">
    <button class="btnG">Supprimer</button>
</form>
