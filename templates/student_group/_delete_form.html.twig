<form method="post" action="{{ path('student_group_delete', {'id': student_group.id}) }}" onsubmit="return confirm('Etes-vous sur de vouloir supprimer ce compte étudiant ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ student_group.id) }}">
    <button class="btnR">SUPPRIMER</button>
</form>
