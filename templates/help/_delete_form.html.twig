<form method="post" action="{{ path('help_delete', {'id': help.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cet indice');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('Delete' ~ help.id) }}">
    <button class="btnR">Supprimer</button>
</form>
