<form method="post" action="{{ path('delete_helps', {'id': challenge.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ challenge.id) }}">
    <br>
    <button class="btnR">Tout supprimer</button>
</form>