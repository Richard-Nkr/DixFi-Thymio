{% extends 'base.html.twig' %}

{% block title %}Création d'un compte Enseignant{% endblock %}

{% block body %}
    {# nous testons si la variable message existe #}
    {% if message is defined %}
        <div class="alert alert-danger">
            {{ message }}
        </div>
    {% endif %}

    {{ include('teacher/_form.html.twig') }}

    <a href="{{ path('teacher_index') }}">back to list</a>
{% endblock %}
